parameters:
  - name: id
    in: path
    required: true
    schema:
      type: int

patch:
  tags:
    - BlobKey
  summary: Blobkey
  operationId: Blobkey
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: object
                $ref: '#/components/schemas/BlobKey'
              
